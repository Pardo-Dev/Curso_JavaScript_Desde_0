const card = document.querySelector('.card');
console.log(card)

// Selecionar etiquetas con query selector

const form = document.querySelector('form');
console.log(form);

// QuerySelectorAll: Selecciona todos los elementos que tienen la clase, id o etiqueta.
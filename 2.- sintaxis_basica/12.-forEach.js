
/* ForEach */
// Recorre cada uno de los elementos de un arreglo

const pendientes = ['Tarea','Comer','Proyecto','Estudiar JavaScript']

pendientes.forEach((tarea)=>{
    console.log(tarea)
})

// Map permite guardar el bucle dentro de una variable

// El bucle forEach es para poder recorrerr listas, mientras que el map para recorrer objetos
